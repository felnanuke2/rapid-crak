import 'package:flutter/material.dart';

/// Paleta de cores para o tema Dark Mode
/// Inspirada em ferramentas técnicas modernas com foco em Rust
class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color surfaceVariant = Color(0xFF2A2A2A);

  // Primary (Rust Orange)
  static const Color primary = Color(0xFFE57373);
  static const Color primaryLight = Color(0xFFFF8A80);
  static const Color primaryDark = Color(0xFFC9184A);

  // Accent (Matrix Green - opcional)
  static const Color accent = Color(0xFF00E676);
  static const Color accentLight = Color(0xFF69F0AE);
  static const Color accentDark = Color(0xFF00BCD4);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color textTertiary = Color(0xFF808080);

  // Semantic
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Tech specific (Terminal/Code colors)
  static const Color terminalGreen = Color(0xFF00E676);
  static const Color terminalGray = Color(0xFFA0A0A0);
  static const Color codeBg = Color(0xFF0D0D0D);

  // Borders & Dividers
  static const Color border = Color(0xFF3A3A3A);
  static const Color divider = Color(0xFF2A2A2A);

  // Overlay & Transparency
  static const Color overlay = Color(0x1AFFFFFF);
  static const Color overlayDark = Color(0x33000000);

  // Progress & Indicators
  static const Color loading = Color(0xFFE57373);
}
